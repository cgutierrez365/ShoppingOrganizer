<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cgutierrez.sashbrook.cbaird.cs134.miracosta.shoppingorganizer">
    <!-- Specify permissions before application -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddStoreActivity"></activity>
        <activity android:name=".NoteDetailsActivity" />
        <activity android:name=".ItemDetailsActivity" />
        <activity android:name=".AddNoteActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".CreditsActivity" />
        <activity android:name=".ItemActivity"
            android:parentActivityName="AddItemActivity"/>
        <activity android:name=".StoresActivity" />
        <activity android:name=".AddCouponActivity" />
        <activity android:name=".CouponActivity"
            android:parentActivityName="AddCouponActivity"/>
        <activity android:name=".CouponDetailsActivity"
            android:parentActivityName=".CouponActivity"/>
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddItemActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBh2wuiqcFLjF1VntpYw51Kxg2NpDR-iaA" />
    </application>

</manifest>